#include<stdio.h>
int main()
{
    int n;
    printf("ENTER THE NUMBER : ");
    scanf("%d",&n);
    for(int i=2;i<=n-1;i++)
    {
        if(n%i==0)
        printf("number %d is not prime",n);
        else
        printf("number prime");
        break;
    }
    return 0;
}
