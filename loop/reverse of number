#include<stdio.h>
int main()
{
    int n,rev=0,lastdigit;
    printf("ENTER THE NUMBER  : ");
    scanf("%d",&n);
    while(n!=0)
    {
        lastdigit=n%10;
        rev=rev*10;
        rev=rev+lastdigit;
        n=n/10;
    }
    printf("reverse of number is %d",rev);
    return 0;
}
