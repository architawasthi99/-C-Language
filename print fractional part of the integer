#include<stdio.h>
int main()
{
    int a,b,q,r,y;
    float m,j;
    printf("enter the decimal value :");
    scanf("%f",&m);
    y=("%d",m);
    j=m-y;
    printf("the fractional part is : %f",j);
    
    
    return 0;
}
